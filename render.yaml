services:
  - type: worker
    name: api-key-service
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: starter
    autoDeploy: true
    envVars:
      - key: JAVA_OPTS
        value: "-Xmx512m"
